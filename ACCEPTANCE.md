# v0.3.0 发布验收记录

## 已完成的本机检查

- `npm test`：45 项通过，覆盖事务重放、幂等提交、撤销依赖、条件事实、承诺进度、实体关联、证据失效、分支隔离、迁移和备份。
- `npm run build`：前后端 JavaScript 语法检查通过。
- `npm run quality`：100 条中文查询中，文本模式 Recall@8 为 0.97，旧版为 0.97；nDCG@8 为 0.925，旧版为 0.924。
- 本机 Node 24 合成数据，5,000 条热文本召回 P95 为 4.5 ms，10,000 条为 8 ms，50,000 条为 57.6 ms；这些是检索核心耗时，不含网络、浏览器和嵌入接口。
- SillyTavern 1.18.0 已加载独立服务端插件，`/capabilities` 返回协议 1、版本 0.3.0；窄屏界面显示嵌入配置、记忆筛选和服务连接成功。

## 尚未完成的实测验收

1. 在手机访问远端宿主及手机本机运行宿主两种场景分别测冷启动、热召回、分页和预览 P95，并记录设备、Node 与浏览器版本。
2. 用用户选择的真实嵌入模型运行独立语义改写验收集，确认 Recall@8 相对旧版至少提升 15 个百分点；同时测超时、限流及模型切换。
3. 用隔离的测试聊天走完整关幕模型生成、逐项拒绝、角色成长刷新、确认、撤销、聊天复制和历史分支 UI 流程。
4. 在旧聊天副本上演练迁移、宿主保存失败后的重试和完整备份恢复。旧幕缺少逐条来源哈希时，历史分支会保守地把该幕置为待整理，避免继承无法验证的日记和成长快照。

本机合成测试结果不能代表手机或真实嵌入模型的性能与质量。
